#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    int n;
    cin >> n;


    vector<int> v;
    int a = 0;
    for (int i = 0; i < n; i++) {
        cin >> a;
        v.push_back(a);
    }

    sort(v.begin(), v.end());

    for (unsigned int i = 0; i < v.size(); i++) cout << v[i] << endl;
}
